server:
  port: 8210
spring:
  application:
    name: micro-gateway-yml
eureka:
  client:
    service-url:
      defaultZone: http://dante:iamdante@${eureka.host:localhost}:${eureka.port:8761}/eureka/